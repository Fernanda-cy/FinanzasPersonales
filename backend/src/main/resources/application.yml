spring:
  application:
    name: FinanzasPersonales
  datasource:
    url: jdbc:mysql://localhost:3306/Finanzas?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8081

# CONFIGURACIÓN PARA DESACTIVAR EUREKA
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

jwt:
  secret: una_clave_secreta_muy_larga_para_que_funcione_el_token_hmac_sha256_segura
  expiration: 3600000